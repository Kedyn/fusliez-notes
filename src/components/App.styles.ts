import { createUseStyles } from "react-jss";

export default createUseStyles({
  container: {
    display: "flex",
    maxWidth: "1600px",
    margin: "0 auto",
    padding: "0.5rem",
    width: "100%",
  },
});
