import { createUseStyles } from "react-jss";

export default createUseStyles({
  ScoresPanel: {
    marginBottom: "1rem",
    alignItems: "center",
    display: "flex",
    justifyContent: "space-between",
    flex: "1 0 16rem",
    maxWidth: "20rem",
  },
});
