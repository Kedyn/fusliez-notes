import { createUseStyles } from "react-jss";

export default createUseStyles({
  ScoresPanel: {
    marginBottom: "1rem",
    alignItems: "center",
    display: "flex",
    justifyContent: "space-between",
    width: "100%",
  },
});
