import { createUseStyles } from "react-jss";

export default createUseStyles({
  footer: {
    display: "flex",
    alignItems: "end",
    justifyContent: "space-around",
  },
  right: {
    marginRight: "0.5rem",
  },
  left: {
    marginLeft: "0.5rem",
  },
});
