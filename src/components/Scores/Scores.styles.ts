import { createUseStyles } from "react-jss";

export default createUseStyles({
  root: {
    margin: "0.5rem 0",
    flex: 1,
    display: "flex",
  },
  title: {
    display: "flex",
    justifyContent: "space-between",
    fontSize: "1.25rem",
  },
  progress: {
    marginBottom: "0.5rem",
  },
});
