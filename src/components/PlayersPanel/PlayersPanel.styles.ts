import { createUseStyles } from "react-jss";

export default createUseStyles({
  PlayersPanel: {
    marginBottom: "1rem",
  },
  PlayersPanelReset: {
    marginTop: "1rem",
  },
  PlayersPanelLock: {
    marginRight: "1rem",
    position: "absolute",
    right: 0,
    bottom: 0,
  },
});
